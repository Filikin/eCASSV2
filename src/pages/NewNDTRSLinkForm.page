<apex:page standardController="NDTRS_Form_2016__c" extensions="CheckForOpenNDTRSLinkFormsController" action="{!countOpenForms}">
    <apex:pageMessages />
    <apex:pageBlock title="{!$Label.OpenHRBFormsTitle}">
        <apex:pageBlockTable value="{!openForms}" var="form">
            <apex:column ><apex:outputLink value="/{!form.id}">{!form.name}</apex:outputLink></apex:column> 
        </apex:pageBlockTable> 
    </apex:pageBlock>
</apex:page>